# Copyright (c) 2026 Mete Balci. All Rights Reserved.

name: Fetch ICON Weather Data

on:
  schedule:
    - cron: '5 * * * *'
  workflow_dispatch:

jobs:
  fetch-icon:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: scripts/requirements.txt

      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      - name: Fetch ICON weather data
        id: icon
        run: |
          cd public
          python ../scripts/fetch-icon.py
          if [ -f icon-wind.bin ] && [ -s icon-wind.bin ]; then
            echo "success=true" >> $GITHUB_OUTPUT
          else
            echo "success=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push
        if: steps.icon.outputs.success == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/
          git commit -m "Update ICON weather data $(date -u +'%Y-%m-%d %H:%M UTC')" || exit 0
          git pull --rebase
          git push
